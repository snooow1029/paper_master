{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install --include=dev && npm list pg || npm install pg@^8.16.3 --save && npm list passport || npm install passport@^0.7.0 passport-google-oauth20@^2.0.0 express-session@^1.17.3 --save && node -e \"require('pg')\" || npm install pg@^8.16.3 --save && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

