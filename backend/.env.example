# Environment Variables
NODE_ENV=development
PORT=5001

# LLM Provider Configuration
# Set the LLM type: "openai", "local", or "disabled"
LLM_TYPE=local

# OpenAI API Configuration (only used if LLM_TYPE=openai)
OPENAI_API_KEY=your_openai_api_key_here

# Local LLM Configuration (only used if LLM_TYPE=local)
LOCAL_LLM_URL=http://localhost:8000
LOCAL_LLM_MODEL=Qwen/Qwen3-4B-Instruct-2507

# Maximum number of concurrent requests to the LLM
# Recommended: 2-5. Higher values may overload the LLM server.
LLM_MAX_CONCURRENCY=5

# GROBID Configuration
# URL for the GROBID server instance used for PDF processing.
GROBID_URL=http://localhost:8070

# Database
DATABASE_URL=database.sqlite

# CORS
CORS_ORIGIN=http://localhost:3000
